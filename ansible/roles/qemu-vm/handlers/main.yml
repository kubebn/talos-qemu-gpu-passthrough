---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes

- name: Restart VM service
  ansible.builtin.systemd:
    name: "talos-qemu@{{ inventory_hostname }}"
    state: restarted
  when: talos_qemu_autostart | default(false)
